# app

The FastAPI application code lives here.

Think of this folder as “the program.” Config and dependency lists stay one level up in `backend/` so they are not mixed with route and database code.

You will add `main.py` and `database.py` in a later step. Do not add them until you are ready to start the API.
