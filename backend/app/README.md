# app

The FastAPI application code lives here.

- `main.py` — creates the FastAPI app and defines the first routes
- `routers/`, `models/`, `schemas/` — empty for now; we fill them in later steps

Config and package lists stay one level up in `backend/` (`requirements.txt`).
