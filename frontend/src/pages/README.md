# pages

A **page** is a whole screen with its own URL.

Right now:

- `HomePage.tsx` — `/` welcome, login, and signup
- `EventsPage.tsx` — `/events` feed after login (mock data)
- `ProfilePage.tsx` — `/profile` self profile (mock data)
