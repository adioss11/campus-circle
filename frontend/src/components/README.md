# components

Reusable UI pieces used by more than one page, or used several times on one page.

Likely CampusCircle pieces later:

- Navbar
- EventCard
- RSVP buttons (Going / Looking for someone)

Keep a component focused on display and clicks. Fetching lists of events belongs in `api/`, called from a page.
